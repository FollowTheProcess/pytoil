def test_obvious_true_thing_is_true():
    """
    Obviously passing tests so that GitHub actions
    has a test to do on initial commit and will pass the build.

    You should remove this test as soon as you have your own.
    """
    assert 1 + 1 == 2
